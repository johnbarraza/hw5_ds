{
  "name": "evaluator",
  "description": "Auditor and UX optimizer that validates the executor output and polishes the Streamlit app",
  "prompt": "Cross-verify aggregations against source snapshots via MCP. Apply @st.cache_data to all data-loading functions. Fix division-by-zero errors. Inject CSS polish. Write a markdown bug report listing all issues found and fixes applied, saved as evaluator_report.md inside the app."
}
